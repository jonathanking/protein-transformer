name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # My package depends
  - numpy

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

