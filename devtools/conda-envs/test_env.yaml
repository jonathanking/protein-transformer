name: test
channels:
  - pytorch
dependencies:
    # Base depends
  - python
  - pip

    # My package depends
  - numpy
  - tqdm
  - pytorch
  - cpuonly
  - scipy
  - joblib


    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
  - pip :
      - ProDy

